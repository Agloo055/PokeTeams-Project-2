<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team</title>
</head>
<body>
    <%- include('../partials/nav') %>
    <h1>Team</h1>
    <ul>
        <% currentUser.teams.forEach((pokemon) => { %>
            <li><%= pokemon.nickname %> (<%= pokemon.pokemon %>) <br>
                <img src="<%= pokemon.img %>" alt="<%= pokemon.pokemon %>">
            </li>
            <form action="/users/<%=currentUser._id%>/teams/team/pokemon/<%=pokemon._id%>?_method=DELETE" method="POST">
                <input type="submit" value="Remove Pokemon">
            </form>
        <% }) %>
    </ul>
    <% if (currentUser.teams.length < 6){ %>
        <form action="/users/<%=currentUser._id%>/teams/team/pokemon/new" method="GET">
            <input type="submit" value="Add Pokemon">
        </form>
    <% } else { %>
        <h3>Maximum amount of pokemon in Team!</h3>
    <% } %>
</body>
</html>