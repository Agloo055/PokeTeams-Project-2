<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team</title>
</head>
<body>
    <%- include('../partials/nav') %>
    <h1><%= pokemon.nickname %> (<%= pokemon.pokemon %>)</h1>
    <img src="<%= pokemon.img %>" alt="<%= pokemon.pokemon %>">
    <a href="/users/<%=currentUser._id%>/teams/team/pokemon/<%=pokemon._id%>/edit">Edit pokemon</a>
    <form action="/users/<%=currentUser._id%>/teams/team/pokemon/<%=pokemon._id%>?_method=DELETE" method="POST">
        <input type="submit" value="Remove Pokemon">
    </form>
    <a href="/users/<%=currentUser._id%>/teams/team/pokemon/">Back to Team</a>
</body>
</html>